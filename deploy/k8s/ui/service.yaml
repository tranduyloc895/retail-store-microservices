apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: retail-store
  labels:
    app: ui
spec:
  type: LoadBalancer
  selector:
    app: ui
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
